# Unified multimodal runtime validated on 2026-03-30.
# Tested Python version: 3.10.19
#
# This dependency set was empirically validated to load processor/backbone for:
# - Qwen2-VL
# - Qwen2.5-VL
# - Qwen3-VL
# - Qwen3.5-VL (0.8B / 2B)
# - Llava-Next
#
# Recommended setup on a fresh machine:
#   python3.10 -m venv nexus-mm
#   source nexus-mm/bin/activate
#   pip install --upgrade pip
#   pip install -r examples/multimodal_retrieval/requirements.txt
#   pip install -e . --no-deps
#
# Notes:
# - Qwen3.5 support requires a transformers main/dev build rather than 4.57.x.
# - Optional speedups such as flash-linear-attention / causal-conv1d are not
#   required for correctness; our validated runtime falls back to torch kernels
#   when they are absent.

torch==2.6.0
torchvision==0.21.0
accelerate==1.13.0
datasets==3.6.0
peft==0.9.0
sentence-transformers==4.1.0
sentencepiece==0.2.0
protobuf==5.29.5
Pillow==11.2.1
decord==0.6.0
faiss-cpu==1.13.2
pytrec_eval==0.5
numpy==2.2.6
pandas==2.2.3
pyarrow==20.0.0
huggingface-hub==1.8.0
tokenizers==0.22.2
safetensors==0.7.0
packaging==26.0
PyYAML==6.0.3
regex==2026.3.32
requests==2.32.3
filelock==3.25.2
fsspec==2026.3.0
psutil==7.0.0
tqdm==4.67.3
typing_extensions==4.15.0
transformers @ git+https://github.com/huggingface/transformers.git
